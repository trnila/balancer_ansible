---
- name: install base packages
  package:
      name: "{{ item }}"
      state: present
  with_items:
    - htop
    - tmux
    - vim
    - minicom
    - socat

- import_tasks: periph.yml
- import_tasks: ssh.yml
