---
- name: install base packages
  package:
      name: "{{ item }}"
      state: present
  with_items:
    - htop
    - tmux
    - vim
    - minicom
    - socat
    - cmake
    - gcc-arm-none-eabi
    - python3-serial
    - python3-pip

- import_tasks: periph.yml
- import_tasks: ssh.yml
